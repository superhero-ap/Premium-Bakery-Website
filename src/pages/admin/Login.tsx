import { useState } from 'react'
import { Link, useNavigate } from 'react-router-dom'
import { supabase } from '../../lib/supabase'

export default function AdminLogin(){const nav=useNavigate();const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const [busy,setBusy]=useState(false); const submit=async(e:React.FormEvent)=>{e.preventDefault();setError('');if(!supabase){setError('Supabase is not configured.');return}setBusy(true);const {error}=await supabase.auth.signInWithPassword({email,password});setBusy(false);if(error)setError('Unable to sign in. Check your credentials.');else nav('/admin')};return <div className="page"><div className="container narrow simple"><div className="eyebrow">STAFF ACCESS</div><h1>Sign in to your bakery workspace.</h1><p>Admin access uses Supabase Auth. Passwords are never stored in the application database.</p><form className="checkout" onSubmit={submit}><div className="form-grid"><label className="span-2">Email *<input type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><label className="span-2">Password *<input type="password" required value={password} onChange={e=>setPassword(e.target.value)}/></label></div>{error&&<p className="form-hint">{error}</p>}<button className="btn primary full" disabled={busy}>{busy?'Signing in…':'Sign in'}</button><Link className="text-btn" to="/">Back to storefront</Link></form></div></div>}
